module Parser
  export to_push

  # Produces a readily executable Push program from a given definition.
  to_push(s::String) = return

  # Lex source code into a sequence of tokens.
  lex(p::String) = return

  # Parse tokens into a Push program.
  parse(p::String) = return

end